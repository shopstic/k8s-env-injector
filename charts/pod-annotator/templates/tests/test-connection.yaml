apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-annotator.fullname" . }}-test-connection"
  labels:
    {{- include "pod-annotator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-annotator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
